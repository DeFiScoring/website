# Jekyll build output — never commit, regenerated on every build
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / bundler
.bundle/
vendor/

# Node / Wrangler
node_modules/
.wrangler/
.dev.vars

# OS / editor noise
.DS_Store
*.log
